[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (e.g. /d/green-room, /checkout) work on reload
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
